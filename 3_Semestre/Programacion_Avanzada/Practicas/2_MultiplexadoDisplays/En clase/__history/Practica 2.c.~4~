#include <18f4550.h>   // Libreria del Microcontrolador
#fuses INTRC, NOWDT, NOPROTECT, NOLVP, CPUDIV1, PLL1, NOMCLR // Fusibles (Configuraciones del microcontrolador)
#use delay(clock = 8M)  // 4M es 4 millones. Establecemos la velocidad va a operar el micro

int vec_c[10] = {0x3F, 0x06, 0x58, 0x4F, 0x66, 0x6D,0x7D,0x07,0x7F,0x67};

void main()
{
   int i;
   
   while(true)
   {
      if(input(pin_a0)
      {
         while(input(pin_a0));
         if(i < 9)
            i++;
         else
            i= 0 ;
      }
      else
         output_d(vec_c[i]);
      
      /*for(i=0;i<10;i++)
      {
         output_d(vec_c[i]);
         delay_ms(500);
      }*/ // Contar del 0 al 9
   }
}
